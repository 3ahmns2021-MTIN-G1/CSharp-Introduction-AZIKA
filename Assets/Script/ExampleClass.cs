
//Bibliotheken /Asembly Referenz/Namenspace
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Deklaration einer Klasse

//Public -> access modifier (Zugriffsrecht) | keyword vorreservierte Schlüssel
//class -> keyword zur deklaration einer Klasse
//ExampleClass -> identifier (name der klasse)
// : MonoBehaviour -> erstellen einer unterklassse von mono behavior
// {} -> Wirkungsbereich/Scope (Was ist alles in der klasse enthalten)
public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
